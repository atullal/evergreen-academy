# Drafts

Work in progress. Files here are NOT published and carry no approval.

- `<slug>.md` — a lesson draft (Penny, edu-writer).
- `<slug>.review.md` — editorial review notes (Quill, edu-editor),
  pass/fail per checklist section.

A lesson leaves this directory only when Quill moves it to `lessons/`
with an approval footer. Nobody else performs that move.
